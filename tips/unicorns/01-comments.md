# Comments

When you want to comment out a chunk of code, you can do so by just removing a
single backslash.

```php
//*
$debug = true;
die(var_dump($debug));
//*/
```

```php
/*
$debug = true;
die(var_dump($debug));
//*/
```
